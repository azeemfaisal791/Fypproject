// Shared domain constants used across models, routes and validation.

// The size options every clothing product can be offered in, in display order.
const PRODUCT_SIZES = ["S", "M", "L", "XL"];

module.exports = { PRODUCT_SIZES };
