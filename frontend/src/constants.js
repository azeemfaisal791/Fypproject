// Size options a product can be offered in, in display order. Mirrors
// PRODUCT_SIZES in backend/utils/constants.js. The sizes a *specific* product
// actually offers come from its `sizes` array from the API; this list is only
// used for ordering/labels.
export const PRODUCT_SIZES = ["S", "M", "L", "XL"];
